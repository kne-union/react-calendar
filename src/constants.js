export const DATE_FORMAT = 'YYYY-MM-DD';
export const TIME_FORMAT = 'HH:mm';
/** Stack panels vertically when container width is below this value (px). */
export const LAYOUT_STACK_BREAKPOINT = 560;
/** Minimum panel viewport height in stacked layout (px), aligned with root min-height. */
export const LAYOUT_MIN_HEIGHT = 560;
